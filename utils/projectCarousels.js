// const carouselContainer = document.querySelector(".carousel-container");
// const carousel = document.querySelector(".carousel");
// let scrollStartPosition = 0;

// carouselContainer.addEventListener("scroll", handleScroll);

// function handleScroll(event) {
//   console.log("helllooo");

//   const scrollPosition = carouselContainer.scrollLeft;

//   if (scrollPosition > scrollStartPosition) {
//     carousel.scrollLeft += 10; // Adjust the scroll speed as desired
//   } else if (scrollPosition < scrollStartPosition) {
//     carousel.scrollLeft -= 10; // Adjust the scroll speed as desired
//   }

//   scrollStartPosition = scrollPosition;
// }
